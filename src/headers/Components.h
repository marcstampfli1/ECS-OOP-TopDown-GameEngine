#pragma once
#include <unordered_map>
#include <string>
#include "Entity.h"

struct Position {
    float x, y, z;
};

struct Velocity {
    float vx = 0, vy = 0;
};

struct Size {
    float w, h;
};



